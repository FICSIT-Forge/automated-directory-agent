{
  "name": "functions",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "build": "pnpm build:ts && pnpm lint && pnpm format:check",
    "build:ts": "tsc",
    "build:watch": "tsc --watch",
    "dev": "tsx --watch 'src/index.ts'",
    "genkit:dev": "genkit start -- pnpm run dev",
    "genkit:emulate": "genkit start -- firebase emulators:start --inspect-functions",
    "build:index": "tsx scripts/buildIndex.ts",
    "verify:index": "tsx scripts/verifyIndex.ts",
    "shell": "pnpm run build && firebase functions:shell",
    "start": "pnpm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@genkit-ai/firebase": "^1.30.1",
    "@genkit-ai/google-genai": "^1.30.1",
    "@google-cloud/functions-framework": "^5.0.2",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2",
    "genkit": "^1.30.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "firebase-functions-test": "^3.4.1",
    "genkit-cli": "^1.30.1",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.1"
  },
  "private": true
}
