# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

# Firebase
firebase-debug.log

# Genkit
.genkit/

# Test coverage reports
coverage/

# Eval query-embedding cache (regenerated on demand)
eval/.query-embedding-cache.json

# Reddit miner output — raw input for hand-labeling, not an artifact
eval/reddit-candidates.json
# mineLogs output — raw input for hand-labeling, not an artifact
eval/log-candidates.json
firestore-debug.log
eval/turn-candidates.json
